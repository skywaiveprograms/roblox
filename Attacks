-- Main Code

Instance.new("Part", workspace)
local part = workspace.Part
part.CFrame = CFrame.new(1,6,0)
part.BrickColor = BrickColor.Blue()
part.Anchored = true

while true do
part.CFrame = part.CFrame * CFrame.Angles(0,.25,0)
end